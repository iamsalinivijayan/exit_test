import React from 'react';
import ReactDOM from 'react-dom';
import Apps from './App';  // Assuming App is the main component of your React app

ReactDOM.render(
  <React.StrictMode>
    <Apps />
  </React.StrictMode>,
  document.getElementById('root')
);
